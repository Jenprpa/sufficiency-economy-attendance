# ⚡ การรีวิวประสิทธิภาพของระบบ (Performance Review)

**Academic Management Platform (AMP)** — โรงเรียนไพวิทยาคาร | Version: v2.2.0

เอกสารนี้ประเมินความเร็วในการทำงาน ขนาดไฟล์ การใช้หน่วยความจำ และระบุจุดที่เป็นคอขวดของระบบในปัจจุบัน พร้อมให้ข้อเสนอแนะในการปรับปรุงประสิทธิภาพในอนาคต

> [!NOTE]
> **Cross-reference:** ดูปัญหาหนี้เทคนิคเพิ่มเติมใน [TECHNICAL_DEBT.md](./TECHNICAL_DEBT.md) และแนวทางการแยกโมดูลใน [ARCHITECTURE_REFACTOR_PLAN.md](./ARCHITECTURE_REFACTOR_PLAN.md)

---

## 1. 📂 พื้นที่โค้ดที่มีขนาดใหญ่ที่สุด (Largest Code Areas)

สัดส่วนขนาดพื้นที่และจุดรวมของโค้ดในโปรเจกต์:
- **`app.js` (657 KB, 11,944 บรรทัด):** เป็นตัวประมวลผลขนาดใหญ่ของหน้าจอ UI ทั้งหมด มีการ parse โค้ดนานเมื่อรันครั้งแรกบนอุปกรณ์พกพา
- **`index.html` (233 KB):** ประกอบด้วยโครงสร้างโมดัลและรูปแบบ View ที่ซ่อนอยู่ด้วยการตั้งค่า CSS `display: none`
- **`style.css` (60 KB):** มีโค้ดจัดรูปแบบเกือบหนึ่งพันบรรทัด

**ข้อเสนอแนะ:** การแยกไฟล์ `app.js` ออกเป็นโมดูลย่อยในอนาคตจะช่วยให้เบราว์เซอร์ดาวน์โหลดและคอมไพล์โค้ดเฉพาะส่วนที่จำเป็นได้เร็วขึ้น (Code Splitting)

---

## 2. 🔍 การคิวรี DOM ซ้ำซ้อน (Repeated DOM Queries)

**ปัญหาที่พบ:**
ระบบมีการค้นหาองค์ประกอบหน้าจอด้วย `document.getElementById` ซ้ำไปซ้ำมาในลูปการเรนเดอร์ เช่น:
```javascript
// ตัวอย่างรูปแบบการเรียกใช้ในลูปเรนเดอร์นักเรียน
for (let s of students) {
    const activeTerm = document.getElementById('term-selector').value; // คอนสตรัคชั่นซ้ำซ้อนในลูป
}
```

**ผลกระทบ:**
การเข้าถึง DOM tree บ่อย ๆ ทำให้แอปช้าลง เนื่องจากเบราว์เซอร์ต้องเสียเวลาค้นหาโครงสร้างในหน่วยความจำ

**ข้อเสนอแนะ:**
เก็บตัวแปรอ้างอิงองค์ประกอบ DOM (DOM Element References) ไว้ที่ระดับคลาสตอนเริ่มต้น เช่น:
```javascript
this.termSelector = document.getElementById('term-selector');
// จากนั้นดึงค่าผ่าน this.termSelector.value
```

---

## 3. 💾 การเข้าถึง Firestore ซ้ำซ้อน (Repeated Firestore Reads)

**ปัญหาที่พบ:**
ไม่มีระบบ caching ข้อมูลอาร์เรย์ที่ดึงจาก Firestore ในหน่วยความจำระดับชั้นความปลอดภัยส่วนกลาง (State Management Cache) ส่งผลให้เวลาสลับหน้าจอ (เช่น สลับจาก Dashboard ไปหน้ารายงานและกลับมา) ระบบต้องทำการคิวรี Firestore ใหม่ทั้งหมด

**ผลกระทบ:**
- เพิ่มสถิติจำนวนการอ่านฐานข้อมูล (Firestore Reads) ซึ่งส่งผลให้มีค่าบริการสูงขึ้น
- หน้าจอมีการกระตุกและโหลดช้าเมื่อกลับมาหน้าเดิม

**ข้อเสนอแนะ:**
สร้าง Local Memory State Cache สำหรับเก็บข้อมูลนักเรียนและประวัติการเช็กชื่อในเซสชันนั้น ๆ และล้างแคชเมื่อมีการอัปเดตข้อมูลจริงเท่านั้น

---

## 🏗️ 4. บล็อกการเรนเดอร์ที่มีภาระงานสูง (Heavy Render Blocks)

**ปัญหาที่พบ:**
เมธอด `renderCheckinStudentList()` และ `generateReport()` ทำการสร้าง DOM Nodes ผ่าน template literals ยาวหลายร้อยบรรทัด และเคลียร์ HTML เก่าด้วย `.innerHTML = ''` ก่อนจะยัด string ใหม่เข้าไปทั้งหมด

**ผลกระทบ:**
เกิดเหตุการณ์ **Layout Thrashing** หรือหน่วงค้างชั่วขณะ (Jank) บนอุปกรณ์สเปกต่ำ เนื่องจากเบราว์เซอร์ต้องคำนวณเลย์เอาต์หน้าจอใหม่ทั้งหมด (Reflow & Repaint)

**ข้อเสนอแนะ:**
- ทยอยเรนเดอร์ข้อมูลแบบแบ่งหน้า (Pagination) หรือโหลดข้อมูลเมื่อเลื่อนลงมาเท่านั้น (Lazy Loading/Infinite Scroll)
- ใช้ `DocumentFragment` ในการประกอบ Node ในหน่วยความจำก่อนอัปเดตลง DOM จริงทีเดียว

---

## 🔄 5. ลูปประมวลผลขนาดใหญ่ (Large Loops)

**ปัญหาที่พบ:**
การตรวจสอบความถูกต้องของข้อมูลผู้ใช้ระบบ (`runUserDataIntegrityCheck()`) และการจัดตารางการหมุนเวียนนักเรียน (`calculateRotation()`) มีการเปรียบเทียบลูปซ้อนลูปแบบ $O(N^2)$ หรือ $O(N \times M)$ บนข้อมูลนักเรียนทั้งหมดและตารางฐาน

**ผลกระทบ:**
เมื่อมีนักเรียนมากกว่า 1,000 คน เบราว์เซอร์อาจเกิดการหยุดชั่วขณะ (Main Thread Blocking) จนขึ้นแจ้งเตือน "Page Unresponsive"

**ข้อเสนอแนะ:**
- ทำการแปลงข้อมูลอาร์เรย์เป็น Map หรือ Index Table ก่อนตรวจความสอดคล้อง เพื่อลดความซับซ้อนของการค้นหาจาก $O(N)$ เหลือ $O(1)$
- ใช้ Web Workers ในการประมวลผลอัลกอริทึมหมุนเวียนฐานการสอน เพื่อไม่ให้กระทบ UI Main Thread

---

## 👂 6. Event Listeners ที่มีจำนวนมาก (Expensive Listeners)

**ปัญหาที่พบ:**
มีการลงทะเบียน listener บนเหตุการณ์ที่มีการทริกเกอร์ถี่มาก เช่น `input` หรือ `keyup` ในระบบค้นหารายชื่อนักเรียนแบบ Real-time โดยส่งค่าไปประมวลผลค้นหาและเรนเดอร์ใหม่ทันทีทุกตัวอักษร

**ผลกระทบ:**
เบราว์เซอร์ประมวลผลเกินขนาดหากครูพิมพ์ข้อความอย่างรวดเร็ว

**ข้อเสนอแนะ:**
ใช้ระบบ **Debouncing** หรือ **Throttling** หน่วงเวลา 250-300ms ก่อนเริ่มทำการค้นหาและเรนเดอร์ข้อมูล

---

## 💾 7. ความเสี่ยงด้านขนาดของ LocalStorage

**ปัญหาที่พบ:**
LocalStorage บนเบราว์เซอร์มีขีดจำกัดขนาดความจุสูงสุดประมาณ 5 MB ปัจจุบันระบบเก็บแคชข้อมูลนักเรียนทั้งหมดและคิวการเช็กชื่อรอการซิงก์ในนี้

**ผลกระทบ:**
หากโรงเรียนมีนักเรียนจำนวนมากและมีการบันทึกภาพถ่ายประกอบการเช็กชื่อแบบ offline ในเครื่องมากเกินไป พื้นที่จัดเก็บอาจเต็มและเบราว์เซอร์จะบล็อกการเขียนข้อมูลใหม่ทั้งหมด ส่งผลให้ข้อมูลออฟไลน์สูญหาย

**ข้อเสนอแนะ:**
- จำกัดขนาดและจำนวนภาพถ่ายที่เช็กชื่อแบบออฟไลน์ หรือจำกัดความละเอียดรูปภาพ
- ย้ายระบบฐานข้อมูลในเครื่องขนาดใหญ่ไปจัดเก็บใน **IndexedDB** แทน (ซึ่งมีความจุมากกว่า 50MB+)

---

## 🌐 8. พฤติกรรมการแคชของ Service Worker

**ปัญหาที่พบ:**
`sw.js` ตั้งค่าการแคชด้วยกลยุทธ์ "Stale-While-Revalidate" หรือ "Cache-First" ทำให้ไม่มีกระบวนการตรวจสอบการหมดอายุของแคชที่เป็นระบบ

**ผลกระทบ:**
ครูอาจได้รับการโหลดไฟล์ `app.js` จากเวอร์ชันเก่า ทำให้ไม่สามารถใช้คุณสมบัติใหม่ที่อัปเดตบน Firebase Hosting ได้ทันที

**ข้อเสนอแนะ:**
- เพิ่ม Cache Version Tag และนำการทำงานอัปเดต Service Worker (Skip Waiting) มาทำงานเมื่อเปิดแอปเวอร์ชันใหม่
