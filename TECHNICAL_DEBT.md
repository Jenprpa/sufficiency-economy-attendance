# ⚠️ การทบทวนหนี้เทคนิคและความเสี่ยง (Technical Debt Review)

**Academic Management Platform (AMP)** — โรงเรียนไพวิทยาคาร | Version: v2.2.0

เอกสารนี้แสดงการวิเคราะห์หนี้เทคนิค (Technical Debt) และจุดเสี่ยงที่มีนัยสำคัญในระบบ เพื่อวางแผนแก้ไขในเฟสการปรับปรุงสถาปัตยกรรมและการพัฒนาในอนาคต

> [!NOTE]
> **Cross-reference:** ดูรายชื่อฟังก์ชันและช่วงบรรทัดใน [APPJS_FUNCTION_INDEX.md](./APPJS_FUNCTION_INDEX.md) และดูแผนการแก้ไของค์รวมใน [ARCHITECTURE_REFACTOR_PLAN.md](./ARCHITECTURE_REFACTOR_PLAN.md)

---

## 📋 รายละเอียดรายการหนี้เทคนิค (Technical Debt Items)

---

### 1. God Class / ขนาดของ `app.js`
- **คำอธิบาย:** คลาส `AttendanceApp` ใน `app.js` มีขนาดบรรทัดยาวถึง **11,944 บรรทัด** ซึ่งทำหน้าที่ครอบคลุมเกือบทุกส่วนของธุรกิจ
- **ผลกระทบ (Impact):** การพัฒนาฟีเจอร์ใหม่มีความเสี่ยงสูงที่จะทำลายความถูกต้องของฟังก์ชันเก่า (High regression risk) ยากต่อการแยกเทส และเบราว์เซอร์ใช้เวลานานในการแยกแยะโค้ด (JS parse time)
- **ระดับความสำคัญ (Priority):** 🔴 High
- **แนวทางแก้ไขที่แนะนำ:** ค่อย ๆ แยกออกเป็นโมดูลย่อยโดยอิงตามแผน [ARCHITECTURE_REFACTOR_PLAN.md](./ARCHITECTURE_REFACTOR_PLAN.md)
- **Sprint ที่เสนอ:** Sprints R1 - R7 (Refactoring Sprints)

---

### 2. ฟังก์ชัน Render มีความยาวมาก (Long Render Functions)
- **คำอธิบาย:** เมธอดประเภทการสร้างหน้าจอ (เช่น `renderCheckin()`, `renderLessonPlanForm()`, `generateReport()`) มีขนาดบรรทัดยาวมาก (บางเมธอดเกิน 500 บรรทัด) เนื่องจากรวมเอาส่วน UI layout และ event binding ไว้ด้วยกัน
- **ผลกระทบ (Impact):** อ่านโค้ดและแก้ไขเลย์เอาต์หน้าจอได้ยากมาก เกิดโค้ดซ้ำซ้อนในการวาดอินเตอร์เฟซ
- **ระดับความสำคัญ (Priority):** 🟡 Medium
- **แนวทางแก้ไขที่แนะนำ:** แยก UI Layout ออกเป็นฟังก์ชันย่อยขนาดเล็ก (Sub-rendering methods) และแยก event handling ออกไป
- **Sprint ที่เสนอ:** Sprints R4, R5 (ระหว่างการสกัดโมดูลเช็กชื่อและแผนกิจกรรม)

---

### 3. โค้ดซ้ำซ้อน (Duplicate Code)
- **คำอธิบาย:** มีการทำสำเนาของเมธอดคำนวณสัญกรณ์ทางวันที่และโครงสร้างโมดัล เช่น `formatThaiDate()` และ `formatThaiDateShort()` หรือการตั้งค่ากล่องแจ้งเตือนความผิดพลาด
- **ผลกระทบ (Impact):** เมื่อต้องการแก้ไขรูปแบบการแสดงผลภาษาไทย ต้องไล่แก้หลายจุด หากแก้ไม่ครบจะเกิดความขัดแย้งของข้อมูล
- **ระดับความสำคัญ (Priority):** 🟡 Medium
- **แนวทางแก้ไขที่แนะนำ:** สกัดฟังก์ชันช่วยระบบแชร์เหล่านี้ออกไปไว้ใน `js/utils.js` ในเฟสแรก
- **Sprint ที่เสนอ:** Sprint R1 (Phase 1)

---

### 4. ตัวแปร Global (Global Variables)
- **คำอธิบาย:** ระบบใช้ global variables บางตัว หรือจัดเก็บคุณสมบัติสำคัญไว้บนออบเจกต์ window (เช่น `window.app`) เพื่อให้ส่วนของ HTML สามารถเรียกใช้ฟังก์ชันได้จากระยะไกล
- **ผลกระทบ (Impact):** เพิ่มความเสี่ยงของ global namespace pollution และอาจเกิดตัวแปรทับซ้อนกันหากมีไลบรารีอื่นภายนอกเพิ่มเข้ามา
- **ระดับความสำคัญ (Priority):** 🟡 Medium
- **แนวทางแก้ไขที่แนะนำ:** หลีกเลี่ยงการประกาศฟังก์ชันตรงเข้า window และใช้ ES Modules import/export ในการเชื่อมโยงข้อมูล
- **Sprint ที่เสนอ:** Sprint R1

---

### 5. การใช้ Inline HTML ใน JavaScript
- **คำอธิบาย:** `app.js` ใช้ template literals ผนวกโค้ด HTML เกือบทั้งหมด เช่น การวาดตาราง การสร้าง badges และกล่อง dialogs
- **ผลกระทบ (Impact):** ไม่มี syntax highlighting สำหรับ HTML ใน IDE, โค้ดอ่านยาก, ผสมปนเปกันระหว่าง logic และ presentation
- **ระดับความสำคัญ (Priority):** 🟡 Medium
- **แนวทางแก้ไขที่แนะนำ:** ปรับปรุงมาใช้ template components หรือสร้างฟังก์ชันจัดการ DOM node โดยตรง
- **Sprint ที่เสนอ:** Sprints R4 - R6

---

### 6. ความเสี่ยงด้าน DOM XSS (innerHTML Usage)
- **คำอธิบาย:** มีการใช้ `.innerHTML = ...` เพื่อเรนเดอร์ข้อมูลโดยตรงจากฟิลด์ข้อความ (เช่น ชื่อนร., topic, note) โดยไม่มีกระบวนการแปลงรหัสหรือล้างสคริปต์
- **ผลกระทบ (Impact):** หากผู้ใช้ที่มีสิทธิ์ไม่ประสงค์ดี กรอกสคริปต์ลงในฟิลด์บันทึกการสอน สคริปต์นั้นจะถูกรันบนอุปกรณ์ของผู้อื่น (เช่น ผู้อำนวยการขณะเปิดดูแผนกิจกรรม)
- **ระดับความสำคัญ (Priority):** 🔴 High
- **แนวทางแก้ไขที่แนะนำ:** เพิ่มฟังก์ชัน `sanitize()` เพื่อแปลงอักขระพิเศษ (HTML Entities) เสมอก่อนนำข้อมูลเขียนลง `.innerHTML`
- **Sprint ที่เสนอ:** Sprint B1 (DOM XSS Sanitization)

---

### 7. การผูก Event ที่กระจัดกระจาย (Event Handling Strategy)
- **คำอธิบาย:** การผูก Event listeners มีทั้งการเขียนผ่าน `bindEvents()` ส่วนกลาง และการฝัง inline `onclick="app.someMethod()"` ใน HTML string
- **ผลกระทบ (Impact):** ไล่ตามลำดับเหตุการณ์ (event flow) ได้ยาก ลบ listener ออกยากเมื่อลบ element ทิ้ง ส่งผลให้มี Memory leak ในระยะยาว
- **ระดับความสำคัญ (Priority):** 🟡 Medium
- **แนวทางแก้ไขที่แนะนำ:** เปลี่ยนมาใช้ Event Delegation ที่ระดับ container หรือใช้ `addEventListener()` จาก JS ทั้งหมดแทน inline onclick
- **Sprint ที่เสนอ:** Sprints R4 - R7

---

### 8. ความขัดแย้งของข้อมูลออฟไลน์ (Offline Sync Conflict Risks)
- **คำอธิบาย:** เมื่อบันทึกการเช็กชื่อแบบออฟไลน์ ระบบจัดเก็บในรูปแบบคิว Local Staging ลำดับการอัปโหลดกลับไป Firestore ใช้แนวทาง "last write wins" เสมอ โดยไม่มีการเช็คข้อมูลล่าสุด
- **ผลกระทบ (Impact):** หากครูสองคนบันทึกห้องเดียวกัน ข้อมูลของครูที่ออนไลน์ภายหลังจะเขียนทับข้อมูลของอีกคนอย่างสมบูรณ์
- **ระดับความสำคัญ (Priority):** 🟡 Medium
- **แนวทางแก้ไขที่แนะนำ:** เพิ่มฟิลด์ timestamp การอัปเดต และใช้ระบบแจ้งเตือนเมื่อเกิดความขัดแย้งของเวลา
- **Sprint ที่เสนอ:** Sprint R2

---

### 9. ความเสี่ยงด้าน Service Worker Cache (sw.js Cache)
- **คำอธิบาย:** `sw.js` แคชไฟล์ `app.js` และ `style.css` เพื่อให้โหลดใช้งานได้เร็วและรองรับออฟไลน์ แต่ไม่มีระบบตัดแคชเวอร์ชันเก่าที่ยืดหยุ่นพอ
- **ผลกระทบ (Impact):** ผู้ใช้อาจมองไม่เห็นการอัปเดตเลย์เอาต์หน้าจอเวอร์ชันใหม่ จนกว่าจะปิดเบราว์เซอร์แล้วเปิดใหม่หรือล้างแคชด้วยตนเอง
- **ระดับความสำคัญ (Priority):** 🟢 Low
- **แนวทางแก้ไขที่แนะนำ:** เปลี่ยน CACHE_NAME อัตโนมัติด้วย versioning tag ทุกครั้งที่มีการ release เวอร์ชันใหม่
- **Sprint ที่เสนอ:** Sprint B2

---

### 10. ค่าใช้จ่ายและประสิทธิภาพการคิวรีข้อมูล (Firestore Read Costs)
- **คำอธิบาย:** บางจุดมีการดึงข้อมูลยกแผง (Full collection query) มาฟิลเตอร์ในฝั่ง Client (เช่น การโหลดข้อมูลนักเรียนทั้งหมดมาฟิลเตอร์ตามห้องเรียน)
- **ผลกระทบ (Impact):** จำนวนการอ่านข้อมูล Firestore (Reads count) สูงเกินความจำเป็น ส่งผลต่อบิลค่าบริการและทำให้แอปช้าลงเมื่อจำนวนนักเรียนเพิ่มขึ้น
- **ระดับความสำคัญ (Priority):** 🟡 Medium
- **แนวทางแก้ไขที่แนะนำ:** ใช้ Query filtering ของ Firestore SDK ในการจำกัดจำนวนข้อมูล เช่น `db.collection('students').where('grade', '==', selectedGrade)`
- **Sprint ที่เสนอ:** Sprint R2

---

### 11. ความยืดหยุ่นบนสมาร์ทโฟน (Mobile Responsiveness)
- **คำอธิบาย:** หน้าตารางตารางหมุนเวียน (Rotation Schedule Grid) และรายงานที่มีตารางขนาดใหญ่ มีลักษณะการล้นของหน้าจอและยืดขยายผิดรูปแบบบนอุปกรณ์ขนาดเล็ก
- **ผลกระทบ (Impact):** ครูไม่สามารถใช้งานระบบสลับฐานการสอนได้อย่างสะดวกผ่านโทรศัพท์มือถือขณะเดินคุมกิจกรรม
- **ระดับความสำคัญ (Priority):** 🟡 Medium
- **แนวทางแก้ไขที่แนะนำ:** ใช้ CSS Flexbox/Grid และการตั้งค่าตารางแบบ scrollable ในแนวนอน ปรับขนาดฟอนต์บนหน้าจอขนาดเล็ก
- **Sprint ที่เสนอ:** Sprint R7

---

### 12. การเข้าถึงของกลุ่มบุคคลพิเศษ (Accessibility Gaps)
- **คำอธิบาย:** ป้ายไอคอนเช็กชื่อ (Badges) มีการระบุความหมายและสถานะด้วยสีเท่านั้น (เช่น เขียว มาเรียน, แดง ขาดเรียน) โดยไม่มีคำอธิบายรูปแบบ Text-Alternative ที่ชัดเจน รวมถึงอินพุตบางจุดขาด ARIA labels
- **ผลกระทบ (Impact):** ครูผู้ใช้งานที่สายตาพร่ามัวหรือบกพร่องทางสีอาจไม่สามารถแยกแยะสถานะการมาเรียนของนักเรียนได้อย่างสะดวก
- **ระดับความสำคัญ (Priority):** 🟢 Low
- **แนวทางแก้ไขที่แนะนำ:** เสริมเลย์เอาต์ด้วยข้อความอธิบายความหมายสั้นข้างไอคอนสถานะ และเพิ่ม ARIA attribute ในทุกจุดอินพุตหลัก
- **Sprint ที่เสนอ:** Sprint R8
